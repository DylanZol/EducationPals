# Captured output

Offline setup verification and generated build output are written here. The
final submission keeps these files so reviewers can compare the recorded run
with their own offline replay.
